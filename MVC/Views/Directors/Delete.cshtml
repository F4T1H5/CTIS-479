@model DirectorResponse

@{
    var containerDivClass = "container";
    ViewData["Title"] = "Director Delete";
}

<div class="@containerDivClass py-4">
    <div class="row justify-content-center">
        <div class="col-lg-7 col-md-9">

            <div class="d-flex justify-content-between align-items-center mb-3">
                <div>
                    <h1 class="h3 mb-1">@ViewData["Title"]</h1>
                    <small class="text-muted">
                        Please confirm this action. This operation cannot be undone.
                    </small>
                </div>
                <div>
                    <a asp-action="Index" class="btn btn-outline-secondary btn-sm">
                        Back to List
                    </a>
                </div>
            </div>

            @if (TempData["Message"] is not null)
            {
                <div class="alert alert-danger alert-dismissible fade show mb-3" role="alert">
                    @TempData["Message"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            }

            @if (Model is not null)
            {
                <div class="card border-0 shadow-lg rounded-4">
                    <div class="card-body p-4 p-md-5">

                        <div class="d-flex align-items-center mb-4">
                            <span class="badge bg-danger-subtle text-danger border border-danger-subtle px-3 py-2 rounded-pill">
                                Delete Confirmation
                            </span>
                        </div>

                        <h4 class="text-danger mb-3">
                            Are you sure you want to delete this director?
                        </h4>

                        <p class="text-muted">
                            You are about to permanently remove the following director:
                        </p>

                        <div class="row mb-3">
                            <div class="col-md-4 col-12 text-muted fw-semibold">
                                @Html.DisplayNameFor(model => model.FullName)
                            </div>
                            <div class="col-md-8 col-12 fw-semibold">
                                @Model.FullName
                            </div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-4 col-12 text-muted fw-semibold">
                                @Html.DisplayNameFor(model => model.IsRetiredF)
                            </div>
                            <div class="col-md-8 col-12">
                                @if (Model.IsRetired)
                                {
                                    <span class="badge bg-secondary-subtle text-secondary border border-secondary-subtle">
                                        @Model.IsRetiredF
                                    </span>
                                }
                                else
                                {
                                    <span class="badge bg-success-subtle text-success border border-success-subtle">
                                        @Model.IsRetiredF
                                    </span>
                                }
                            </div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-4 col-12 text-muted fw-semibold">
                                @Html.DisplayNameFor(model => model.IsRetired)
                            </div>
                            <div class="col-md-8 col-12">
                                @Html.DisplayFor(model => model.IsRetired)
                            </div>
                        </div>

                        @* Many-to-many example (kept, styled) *@
                        @*
                        <div class="row mb-3">
                            <div class="col-md-4 col-12 text-muted fw-semibold">
                                <b>@Html.DisplayNameFor(model => model.Entitys)</b>
                            </div>
                            <div class="col-md-8 col-12">
                                @Html.Raw(Model.Entitys)
                            </div>
                        </div>
                        *@

                        <hr class="my-4" />

                        <form asp-action="Delete">
                            @Html.AntiForgeryToken()
                            <input type="hidden" asp-for="Id" />

                            <div class="d-flex flex-wrap gap-2 justify-content-end">
                                <input type="submit" value="Yes, delete director" class="btn btn-danger" />
                                <a asp-action="Index" class="btn btn-outline-secondary">
                                    No, go back
                                </a>
                            </div>
                        </form>

                    </div>
                </div>
            }

        </div>
    </div>
</div>
